{
  "name": "saveoursouls",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:api": "wrangler pages dev --compatibility-date=2024-06-01 --port 8788 .",
    "build": "vite build",
    "preview": "vite preview",
    "cf:project:create": "wrangler pages project create saveoursouls-pages --production-branch=main",
    "cf:deploy": "wrangler pages deploy dist --project-name saveoursouls-pages"
  },
  "dependencies": {
    "otplib": "^12.0.1",
    "resend": "^6.2.2",
    "vue": "^3.5.22",
    "vue-router": "^4.4.5"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.1",
    "vite": "^7.1.12",
    "wrangler": "^4.44.0"
  }
}
